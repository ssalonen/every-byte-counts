# Pins the fastlane toolchain so CI and local machines resolve the same gems.
# Commit the Gemfile.lock produced by the first successful CI run to lock exact
# versions (this environment can't run `bundle` to generate it up front).
source "https://rubygems.org"

gem "fastlane"
